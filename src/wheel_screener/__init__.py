"""Cash-secured-put / wheel options screener."""

# Single source of truth for the version. pyproject.toml derives it (hatchling dynamic version)
# and the API reads it, so a release is a one-line bump here.
__version__ = "1.3.0"
